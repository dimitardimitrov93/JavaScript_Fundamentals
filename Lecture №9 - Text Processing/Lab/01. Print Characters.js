function charFromString(stringInput) {
    
    for (const char of stringInput) {
        console.log(char);
    }

    // stringInput.split('').forEach(char => console.log(char));
}

charFromString('AWord');